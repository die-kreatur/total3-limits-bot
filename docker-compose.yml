services:
  tg:
    image: local/limits-bot:latest
    restart: unless-stopped
    build:
      context: .
    environment:
      - RUST_LOG=debug
    depends_on:
      - redis
    volumes:
      - ./configs:/app/configs:ro
  redis:
    image: redis
    restart: unless-stopped
    volumes:
      - redis-data:/data
volumes:
  redis-data:
